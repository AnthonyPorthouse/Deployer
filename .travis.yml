language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7
  - hhvm

matrix:
  allow_failures:
    - php: 7
    - php: hhvm

install:
  - travis_retry composer install --no-interaction --prefer-source

script:
  -  phpunit
